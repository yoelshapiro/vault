# PR 102398 CI Fixes

- Date: 2026-05-13
- Branch: `03-20-si-group-interleave-control-support`
- PR: 102398
- Change type: Test / Config
- Areas: `wayve/ai/si`

## Summary

Fixed the failing SI presubmit checks for PR 102398 without committing.

## Changes

- Regenerated `wayve/ai/si/test/test_config_inputs/reference_rl.yaml` for the current RL baseline schema.
- Added the missing `DrivingControlKey` import and updated offline RL deployment-config tests to cover behavior control, interleave control, and mitigation request independently.
- Updated BC training deployment-config test stubs for new deployment fields and `_get_driving_controls_keys`.
- Added a concrete test subclass for `DeploymentWrapperImpl` now that the base wrapper is abstract.

## Verification

- `bazel test //wayve/ai/si:test_config_py_test_core --test_arg='-k=test_regression' --test_output=errors`
- `bazel test //wayve/ai/si:py_lint_flake8 --test_output=errors`
- `bazel test //wayve/ai/si:py_test_test_deployment_core --test_arg=--no-cov --test_output=errors`
- `bazel test //wayve/ai/si:py_test_test_offline_rl_core --test_arg=--no-cov --test_output=errors`
- `bazel test //wayve/ai/si:py_test_test_training_core --test_arg=--no-cov --test_env=GIT_HASH=local-test --test_env=GIT_DIFF_BASE64=Cg== --test_output=errors`

Notes:
- Refreshed ACR logins after an initial `401 Unauthorized` while analyzing `py_lint_flake8`.
- Pulled the local Git LFS fixture `wayve/ai/si/test/models/data/test_batch.pt`; it is not a source diff.
