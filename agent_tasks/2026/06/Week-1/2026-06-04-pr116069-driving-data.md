# PR 116069 Driving Data Update

- Branch: `codex/pr-116069-driving-data`
- PR: `https://github.com/wayveai/WayveCode/pull/116069`
- Source branch: `boris/parking-past30-no-standstill-gear-aug/main_cherrypick_new_driving`
- Change type: Parking config update, uncommitted.

## Summary

Updated `/workspace/WayveCode/wayve/ai/si/configs/parking/parking_config.py` on top of PR 116069 to port the newer driving data wiring from the source branch without wholesale-copying unrelated config changes.

## Changes

- Added `PARKING_DIFFUSION_DRIVING_TRAIN_PARTITIONS` and `PARKING_DIFFUSION_DRIVING_VAL_PARTITIONS` aliases to the release driving partitions.
- Added `parking_bc_new_driving_datamodule_cfg`, combining the updated driving train/validation partitions with the existing PUDO buckets from `parking_pudo_bc_datamodule_cfg`.
- Updated `parking_diffusion_datamodule_cfg` to use the release driving materialisation version and updated driving partitions.
- Ported the diffusion parking/PUDO data mix from the source branch, including `PARKING_DIFFUSION_PUDO_WEIGHT`.
- Removed the obsolete hardcoded diffusion driving materialisation and old GC data-root constant from the diffusion datamodule path.

## Validation

- `git diff --check`: passed.
- `bazel test //wayve/ai/si:py_lint_ruff`: passed.
- `bazel test //wayve/ai/si:test_config_py_test_core --test_arg=-k --test_arg=test_regression`: failed on baseline BC snapshot drift unrelated to the parking data patch. The actual baseline config includes newer `ParkingDataConfig` cleanup/route fields and `lr_scheduler_num_steps` compared with `reference_bc.yaml`.
